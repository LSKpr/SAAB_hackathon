// Terrain (coastlines, islands, sea, grid) ships as part of the imported
// `boreal-passage.svg`. This file exists for future symmetry — e.g. dynamic
// terrain overlays, weather, or ECM zones.
export default function TerrainLayer() {
  return null;
}
